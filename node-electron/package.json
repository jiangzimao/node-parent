{
  "name": "node-electron",
  "version": "1.0.0",
  "description": "node electron",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . nodeElectron --win --out ../NodeElectron --arch=x64 --app-version=1.0.0 --electron-version=2.0.2"
  },
  "author": "dcs v9",
  "license": "ISC",
  "dependencies": {
    "electron": "^2.0.2",
    "electron-rebuild": "^1.7.3"
  }
}
